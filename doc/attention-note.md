Q=XW_{Q}
Qは各単語にあるQueryベクター、「私は“どんな性質の情報”を探していますか？」。 例. 「食べる」なら行為の主体を探す。
Kはその単語の性質。
QK^TはQが求めているものにどれくらい近いのかの値　、「あなたは私の探している性質をどれくらい持っていますか？」
softmaxで重みを作り分布を作る。だから全部足すと1になる。
Vは動的な重みで単語を混ぜて新しい情報を作る？？




Transformerでは
embedding空間（意味空間）
Q空間（検索空間）
K空間（タグ空間）
V空間（情報空間）

QKの意味するところは　Q/K空間（関係空間）
例
単語 i が単語 j をどれくらい必要としているか
つまり、食べる が 私 をどれくらい必要としているか

embedding空間: リンゴ と オレンジ は近い
QK空間: 
食べる → 主語
食べる → 目的語


🎯 Self-Attentionの本質

各単語は：

他の単語を重み付き平均して
自分をアップデートする

Self-Attention は：
単語を文脈依存ベクトルに変換する装置

なんのために？？
次の予測を正しくするため。


だから

attentionって要するに

```
食べる -> 「主語と目的語を含んだ食べる」
```

というふうに自分自身を更新するということ


# Attentionの洞察は：　「意味とは、周囲との関係で決まる」
Attentionは
❌ 人間の理解を再現したわけではない
✅ 文脈依存性を線形代数で実装した



```
そしてざっくりtransformerは

1. attention headを複数枚作り　それぞれの並列ですすめる。
2. 出てきた出力をattention -> MLP(非線形)と直列に繋げる。(Attention（混ぜる）→ MLP（考える）)
3. それをblockとして縦に積む。
```

これを何回もやっていくということ。

